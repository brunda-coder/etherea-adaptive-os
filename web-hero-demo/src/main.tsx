import React from 'react'
import ReactDOM from 'react-dom/client'
import { HeroDemo } from './App'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HeroDemo />
  </React.StrictMode>,
)
