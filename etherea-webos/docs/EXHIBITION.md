# Exhibition Playbook

Run demo:

```bash
cd etherea-webos
npm install
npm run build
npm run selfcheck
npm run dev -w @etherea/web
```

Laptop demo: boot aurora ring, avatar speak demo, drawing/pdf/coding modes, agent works, settings presets.
Phone demo: build Android APK from release workflow and install.
