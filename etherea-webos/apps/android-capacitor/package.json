{
  "name": "@etherea/android-capacitor",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "sync": "node scripts/ensure_android_platform.mjs && cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
