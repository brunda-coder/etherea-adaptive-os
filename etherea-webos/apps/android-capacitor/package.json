{
  "name": "etherea-android-capacitor",
  "private": true,
  "scripts": {
    "build:web": "npm run build -w @etherea/web",
    "sync": "npx cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
